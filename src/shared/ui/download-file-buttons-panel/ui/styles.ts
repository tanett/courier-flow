import { createStyles, rem } from '@mantine/core';

export const useStyles = createStyles(() => ({
    buttonPanelWrapper: {
        justifyContent: 'center',
        gap: rem(12),
        marginTop: rem(24),
        paddingBottom: rem(4),
    },
}
));
