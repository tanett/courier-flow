import { typeNotificationState } from './types';

// TODO: remove import { NOTIFICATION_TYPES } from '../../../shared/ui/page-notification';

export const initialNotificationState: typeNotificationState = {
    notificationList: [

        // TODO: remove { id: 1, type: NOTIFICATION_TYPES.ERROR, message: 'Test message long long long long long long long long long long long long long long long long long long long long long long long long long long long' }
    ],
};
