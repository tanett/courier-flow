import { typeFilterForm } from '../types/types';

export const initialStoresFilterForm: typeFilterForm = { type: null, createdAt: [ null, null ] };

export const storesFilterForm = { initialValues: initialStoresFilterForm };
