import { typeCashDeskFilterForm } from '../types/types';

export const initialCashDeskFilterForm: typeCashDeskFilterForm = { storeId: null };

export const cashDeskFilterForm = { initialValues: initialCashDeskFilterForm };
