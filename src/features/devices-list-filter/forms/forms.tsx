import { typeFilterForm } from '../types/types';

export const initialDevicesFilterForm: typeFilterForm = { model: '' };

export const devicesFilterForm = { initialValues: initialDevicesFilterForm };
