import { createStyles, rem } from '@mantine/core';


export const useStyles = createStyles((theme) => ({
        iconContainer: {
            width: '24px',
            height: '24px',
        },
    gray:{color: theme.colors.gray[5],},
    primary:{color: theme.colors.primary[5],},

    })
);
